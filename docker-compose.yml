version: '3.8'
services:
  azurite:
    image: mcr.microsoft.com/azure-storage/azurite
    container_name: azurite
    volumes:
      - ./azurite:/data
    ports:
      - "10000:10000"
      - "10001:10001"
      - "10002:10002"
    command: azurite --location /data --debug /data/debug.log

  backend:
    build: ./backend
    container_name: backend
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - AzureStorage__ConnectionString=DefaultEndpointsProtocol=http;AccountName=devstoreaccount1;AccountKey=Eby8vdM02xNOcqFeqGh0pJ+0m+E0xKZb6Y9r5n9X5y6l9r9F1X1y0z2b9X0N4u6+g==;BlobEndpoint=http://azurite:10000/devstoreaccount1;
    depends_on:
      - azurite
    ports:
      - "8080:8080"
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: frontend
    environment:
      - NEXT_PUBLIC_API_URL=http://backend:8080
    depends_on:
      - backend
    ports:
      - "3000:3000"
    restart: unless-stopped
